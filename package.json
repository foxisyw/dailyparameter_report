{
  "name": "daily-parameter-review",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r public/data dist/data 2>/dev/null || true && cp public/how-it-works.html dist/ 2>/dev/null || true",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
